name: Build ImmortalWrt H5000M

on:
  workflow_dispatch:
    inputs:
      enable_adguardhome:
        description: '启用 AdGuardHome'
        required: false
        default: false
        type: boolean
      enable_openclash:
        description: '启用 OpenClash'
        required: false
        default: false
        type: boolean
      enable_nikki:
        description: '启用 Nikki'
        required: false
        default: false
        type: boolean
      enable_upnp:
        description: '启用 UPnP'
        required: true
        default: true
        type: boolean
      enable_vlmcsd:
        description: '启用 VLMCSd'
        required: false
        default: false
        type: boolean
      enable_mosdns:
        description: '启用 MosDNS'
        required: false
        default: false
        type: boolean
      enable_dockerman:
        description: '启用 DockerMan'
        required: false
        default: false
        type: boolean
      enable_qmodem_next:
        description: '启用 QModem Next (新版)'
        required: true
        default: true
        type: boolean
      enable_qmodem:
        description: '启用 QModem (旧版)'
        required: false
        default: false
        type: boolean
      enable_mwan:
        description: '启用 MWAN (多WAN负载均衡)'
        required: true
        default: true
        type: boolean
      enable_homeproxy:
        description: '启用 HomeProxy'
        required: true
        default: true
        type: boolean
      enable_adbyby_plus:
        description: '启用 Adbyby Plus'
        required: false
        default: false
        type: boolean
      enable_original_modem:
        description: '启用原版 Modem'
        required: false
        default: false
        type: boolean
      enable_netspeedtest:
        description: '启用 netspeedtest'
        required: false
        default: false
        type: boolean
      enable_at_webserver:
        description: '启用 at-webserver'
        required: false
        default: false
        type: boolean
      enable_zerotier:
        description: '启用 zerotier'
        required: false
        default: false
        type: boolean
      enable_watchcat:
        description: '启用 watchcat'
        required: false
        default: false
        type: boolean
      enable_easytier:
        description: '启用 easytier'
        required: false
        default: false
        type: boolean
      enable_lucky:
        description: '启用 lucky'
        required: false
        default: false
        type: boolean
      enable_bandix:
        description: '启用 Bandix'
        required: true
        default: true
        type: boolean
      enable_daed:
        description: '启用 daed'
        required: false
        default: false
        type: boolean
      enable_etherwake:
        description: '启用 EtherWake (网络唤醒)'
        required: false
        default: false
        type: boolean
      enable_wireguard:
        description: '启用 WireGuard'
        required: false
        default: false
        type: boolean
  repository_dispatch:
    types:
      - Source Code Update

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

env:
  REPO_URL: https://github.com/561410590/immortalwrt-mt798x-6.6
  REPO_BRANCH: mtwifi-sync-20260321-002700
  CONFIG_URL: https://raw.githubusercontent.com/padavanonly/immortalwrt-mt798x-6.6/refs/heads/mt798x-mt799x-6.6-mtwifi/defconfig/mt7987_mt7992.config
  SOURCE_DIR: immortalwrt
  TZ: Asia/Shanghai

jobs:
  build_firmware:
    runs-on: ubuntu-22.04
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 深度清理磁盘空间
        run: |
          echo "🧹 执行深度清理以释放磁盘空间..."
          
          # 删除 Docker 资源
          docker rmi $(docker images -q) 2>/dev/null || true
          docker system prune -f 2>/dev/null || true
          
          # 删除大型组件
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /etc/mysql /etc/php \
            /usr/local/share/boost /usr/share/swift /opt/hostedtoolcache /usr/local/.ghcup \
            /usr/local/graalvm /imagegeneration /usr/local/share /opt /var/cache/apt/archives/* \
            /etc/apt/sources.list.d/* 2>/dev/null || true
          
          # 卸载不需要的软件包
          sudo apt-get -y purge azure-cli* docker* ghc* zulu* hhvm* llvm* firefox* \
            google* dotnet* aspnetcore* powershell* openjdk* adoptopenjdk* mysql* php* \
            mongodb* moby* snap* 2>/dev/null || true
          
          # 清理包管理器和临时文件
          sudo apt-get autoremove --purge -y && sudo apt-get clean && sudo apt-get autoclean
          sudo rm -rf /tmp/* /var/tmp/* /var/log/* ~/.cache/* 2>/dev/null || true
          
          AVAIL_GB=$(df -BG / | tail -1 | awk '{print $4}' | sed 's/G//')
          echo "✅ 清理后可用磁盘空间: ${AVAIL_GB}GB"
          
          if [ "$AVAIL_GB" -lt 6 ]; then
            echo "❌ 磁盘空间不足，需要至少6GB"
            exit 1
          fi

      - name: 安装编译依赖
        run: |
          echo "📦 安装编译依赖..."
          sudo apt-get update -qq
          sudo apt-get install -y build-essential git ccache python3 python3-pip \
            libncurses5-dev libssl-dev libgmp3-dev libmbedtls-dev \
            golang-go autoconf automake libtool patch make gcc g++ gawk gettext unzip file wget \
            jq clang llvm npm

          # Rust 环境配置 (使用 rustup)
          echo "🔧 配置 Rust 环境..."
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          source "$HOME/.cargo/env"
          rustup target add aarch64-unknown-linux-musl
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH
          
          # 设置 ccache
          echo "📦 设置 ccache..."
          export PATH="/usr/lib/ccache:$PATH"
          echo "PATH=/usr/lib/ccache:$PATH" >> $GITHUB_ENV
          ccache --set-config=max_size=2G
          ccache --version || true

      - name: 恢复插件配置缓存
        id: restore-config
        uses: actions/cache/restore@v4
        with:
          path: config/plugins.json
          key: plugins-config-latest
          restore-keys: |
            plugins-config-

      - name: 恢复编译缓存
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.SOURCE_DIR }}/dl
            ${{ env.SOURCE_DIR }}/staging_dir
            ~/.ccache
          key: ${{ runner.os }}-build-${{ env.REPO_BRANCH }}-${{ hashFiles('feeds.conf.default', 'h5000m.extra.config') }}-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.REPO_BRANCH }}-${{ hashFiles('feeds.conf.default', 'h5000m.extra.config') }}-
            ${{ runner.os }}-build-${{ env.REPO_BRANCH }}-

      - name: 设置环境变量
        id: set-env
        run: |
          echo "🔧 设置环境变量..."
          
          mkdir -p config
          EVENT_NAME="${{ github.event_name }}"
          if [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            echo "🧭 手动触发，优先使用 UI 传入的 inputs"
            ENABLE_ADGUARDHOME="${{ github.event.inputs.enable_adguardhome || 'false' }}"
            ENABLE_OPENCLASH="${{ github.event.inputs.enable_openclash || 'false' }}"
            ENABLE_NIKKI="${{ github.event.inputs.enable_nikki || 'false' }}"
            ENABLE_UPNP="${{ github.event.inputs.enable_upnp || 'false' }}"
            ENABLE_VLMCSD="${{ github.event.inputs.enable_vlmcsd || 'false' }}"
            ENABLE_MOSDNS="${{ github.event.inputs.enable_mosdns || 'false' }}"
            ENABLE_DOCKERMAN="${{ github.event.inputs.enable_dockerman || 'false' }}"
            ENABLE_QMODEM_NEXT="${{ github.event.inputs.enable_qmodem_next || 'false' }}"
            ENABLE_QMODEM="${{ github.event.inputs.enable_qmodem || 'false' }}"
            ENABLE_MWAN="${{ github.event.inputs.enable_mwan || 'false' }}"
            ENABLE_HOMEPROXY="${{ github.event.inputs.enable_homeproxy || 'false' }}"
            ENABLE_ADBYBY_PLUS="${{ github.event.inputs.enable_adbyby_plus || 'false' }}"
            ENABLE_ORIGINAL_MODEM="${{ github.event.inputs.enable_original_modem || 'false' }}"
            ENABLE_NETSPEEDTEST="${{ github.event.inputs.enable_netspeedtest || 'false' }}"
            ENABLE_AT_WEBSERVER="${{ github.event.inputs.enable_at_webserver || 'false' }}"
            ENABLE_ZEROTIER="${{ github.event.inputs.enable_zerotier || 'false' }}"
            ENABLE_WRTBWMON="${{ github.event.inputs.enable_wrtbwmon || 'false' }}"
            ENABLE_WATCHCAT="${{ github.event.inputs.enable_watchcat || 'false' }}"
            ENABLE_EASYTIER="${{ github.event.inputs.enable_easytier || 'false' }}"
            ENABLE_LUCKY="${{ github.event.inputs.enable_lucky || 'false' }}"
            ENABLE_BANDIX="${{ github.event.inputs.enable_bandix || 'false' }}"
            ENABLE_DAED="${{ github.event.inputs.enable_daed || 'false' }}"
            ENABLE_ETHERWAKE="${{ github.event.inputs.enable_etherwake || 'false' }}"
            ENABLE_WIREGUARD="${{ github.event.inputs.enable_wireguard || 'false' }}"
          else
            if [ -f "config/plugins.json" ]; then
              echo "📂 自动触发，发现缓存配置，读取 config/plugins.json"
              # 使用 jq 解析 JSON
              eval $(jq -r 'to_entries | map(select(.key | startswith("enable_")) | select(.value == true or .value == "true") | .key | ascii_upcase + "=true") | .[]' config/plugins.json)
            else
              echo "🔄 自动触发且无缓存，使用默认空配置"
              # 默认全为 false，无需额外操作，下方变量未定义时默认为空（或需显式设为 false）
            fi
          fi
          
          # 确保所有变量都有值（默认为 false）
          : ${ENABLE_ADGUARDHOME:=false}
          : ${ENABLE_OPENCLASH:=false}
          : ${ENABLE_NIKKI:=false}
          : ${ENABLE_UPNP:=false}
          : ${ENABLE_VLMCSD:=false}
          : ${ENABLE_MOSDNS:=false}
          : ${ENABLE_DOCKERMAN:=false}
          : ${ENABLE_QMODEM_NEXT:=false}
          : ${ENABLE_QMODEM:=false}
          : ${ENABLE_MWAN:=false}
          : ${ENABLE_HOMEPROXY:=false}
          : ${ENABLE_ADBYBY_PLUS:=false}
          : ${ENABLE_ORIGINAL_MODEM:=false}
          : ${ENABLE_NETSPEEDTEST:=false}
          : ${ENABLE_AT_WEBSERVER:=false}
          : ${ENABLE_ZEROTIER:=false}
          : ${ENABLE_WRTBWMON:=false}
          : ${ENABLE_WATCHCAT:=false}
          : ${ENABLE_EASYTIER:=false}
          : ${ENABLE_LUCKY:=false}
          : ${ENABLE_BANDIX:=false}
          : ${ENABLE_DAED:=false}
          : ${ENABLE_ETHERWAKE:=false}
          : ${ENABLE_WIREGUARD:=false}
          
          # 输出配置到 GITHUB_OUTPUT
          echo "enable_adguardhome=$ENABLE_ADGUARDHOME" >> $GITHUB_OUTPUT
          echo "enable_openclash=$ENABLE_OPENCLASH" >> $GITHUB_OUTPUT
          echo "enable_nikki=$ENABLE_NIKKI" >> $GITHUB_OUTPUT
          echo "enable_upnp=$ENABLE_UPNP" >> $GITHUB_OUTPUT
          echo "enable_vlmcsd=$ENABLE_VLMCSD" >> $GITHUB_OUTPUT
          echo "enable_mosdns=$ENABLE_MOSDNS" >> $GITHUB_OUTPUT
          echo "enable_dockerman=$ENABLE_DOCKERMAN" >> $GITHUB_OUTPUT
          echo "enable_qmodem_next=$ENABLE_QMODEM_NEXT" >> $GITHUB_OUTPUT
          echo "enable_qmodem=$ENABLE_QMODEM" >> $GITHUB_OUTPUT
          echo "enable_mwan=$ENABLE_MWAN" >> $GITHUB_OUTPUT
          echo "enable_homeproxy=$ENABLE_HOMEPROXY" >> $GITHUB_OUTPUT
          echo "enable_adbyby_plus=$ENABLE_ADBYBY_PLUS" >> $GITHUB_OUTPUT
          echo "enable_original_modem=$ENABLE_ORIGINAL_MODEM" >> $GITHUB_OUTPUT
          echo "enable_netspeedtest=$ENABLE_NETSPEEDTEST" >> $GITHUB_OUTPUT
          echo "enable_at_webserver=$ENABLE_AT_WEBSERVER" >> $GITHUB_OUTPUT
          echo "enable_zerotier=$ENABLE_ZEROTIER" >> $GITHUB_OUTPUT
          echo "enable_wrtbwmon=$ENABLE_WRTBWMON" >> $GITHUB_OUTPUT
          echo "enable_watchcat=$ENABLE_WATCHCAT" >> $GITHUB_OUTPUT
          echo "enable_easytier=$ENABLE_EASYTIER" >> $GITHUB_OUTPUT
          echo "enable_lucky=$ENABLE_LUCKY" >> $GITHUB_OUTPUT
          echo "enable_bandix=$ENABLE_BANDIX" >> $GITHUB_OUTPUT
          echo "enable_daed=$ENABLE_DAED" >> $GITHUB_OUTPUT
          echo "enable_etherwake=$ENABLE_ETHERWAKE" >> $GITHUB_OUTPUT
          echo "enable_wireguard=$ENABLE_WIREGUARD" >> $GITHUB_OUTPUT
          
          # 显示最终配置
          echo "📋 最终使用的配置:"
          echo "• AdGuardHome: $ENABLE_ADGUARDHOME"
          echo "• OpenClash: $ENABLE_OPENCLASH"
          echo "• Nikki: $ENABLE_NIKKI"
          echo "• UPnP: $ENABLE_UPNP"
          echo "• VLMCSd: $ENABLE_VLMCSD"
          echo "• MosDNS: $ENABLE_MOSDNS"
          echo "• DockerMan: $ENABLE_DOCKERMAN"
          echo "• QModem Next: $ENABLE_QMODEM_NEXT"
          echo "• QModem: $ENABLE_QMODEM"
          echo "• MWAN: $ENABLE_MWAN"
          echo "• HomeProxy: $ENABLE_HOMEPROXY"
          echo "• Adbyby Plus: $ENABLE_ADBYBY_PLUS"
          echo "• 原版 Modem: $ENABLE_ORIGINAL_MODEM"
          echo "• NetSpeedTest: $ENABLE_NETSPEEDTEST"
          echo "• AT WebServer: $ENABLE_AT_WEBSERVER"
          echo "• ZeroTier: $ENABLE_ZEROTIER"
          echo "• WRtBWMon: $ENABLE_WRTBWMON"
          echo "• WatchCat: $ENABLE_WATCHCAT"
          echo "• EasyTier: $ENABLE_EASYTIER"
          echo "• Lucky: $ENABLE_LUCKY"
          echo "• Bandix: $ENABLE_BANDIX"
          echo "• Daed: $ENABLE_DAED"
          echo "• EtherWake: $ENABLE_ETHERWAKE"
          echo "• WireGuard: $ENABLE_WIREGUARD"

      - name: 生成标准数据源 plugins.json
        run: |
          mkdir -p config
          echo "🧾 生成标准 plugins.json (SSOT)"
          CURRENT_JSON=$(cat << 'EOF'
          {
            "enable_adguardhome": "${{ steps.set-env.outputs.enable_adguardhome }}",
            "enable_openclash": "${{ steps.set-env.outputs.enable_openclash }}",
            "enable_nikki": "${{ steps.set-env.outputs.enable_nikki }}",
            "enable_upnp": "${{ steps.set-env.outputs.enable_upnp }}",
            "enable_vlmcsd": "${{ steps.set-env.outputs.enable_vlmcsd }}",
            "enable_mosdns": "${{ steps.set-env.outputs.enable_mosdns }}",
            "enable_dockerman": "${{ steps.set-env.outputs.enable_dockerman }}",
            "enable_qmodem_next": "${{ steps.set-env.outputs.enable_qmodem_next }}",
            "enable_qmodem": "${{ steps.set-env.outputs.enable_qmodem }}",
            "enable_mwan": "${{ steps.set-env.outputs.enable_mwan }}",
            "enable_homeproxy": "${{ steps.set-env.outputs.enable_homeproxy }}",
            "enable_adbyby_plus": "${{ steps.set-env.outputs.enable_adbyby_plus }}",
            "enable_original_modem": "${{ steps.set-env.outputs.enable_original_modem }}",
            "enable_netspeedtest": "${{ steps.set-env.outputs.enable_netspeedtest }}",
            "enable_at_webserver": "${{ steps.set-env.outputs.enable_at_webserver }}",
            "enable_zerotier": "${{ steps.set-env.outputs.enable_zerotier }}",
            "enable_wrtbwmon": "${{ steps.set-env.outputs.enable_wrtbwmon }}",
            "enable_watchcat": "${{ steps.set-env.outputs.enable_watchcat }}",
            "enable_easytier": "${{ steps.set-env.outputs.enable_easytier }}",
            "enable_lucky": "${{ steps.set-env.outputs.enable_lucky }}",
            "enable_bandix": "${{ steps.set-env.outputs.enable_bandix }}",
            "enable_daed": "${{ steps.set-env.outputs.enable_daed }}",
            "enable_etherwake": "${{ steps.set-env.outputs.enable_etherwake }}",
            "enable_wireguard": "${{ steps.set-env.outputs.enable_wireguard }}"
          }
          EOF
          )
          echo "$CURRENT_JSON" | jq '
            with_entries(select(.key | startswith("enable_"))
            | .value = (
                if (.value|type)=="string" then 
                  (.value | ascii_downcase == "true")
                else 
                  .value 
                end
              )
            )' > config/plugins.json
          echo "✅ plugins.json 已生成"
          cat config/plugins.json

      - name: 源码和依赖准备
        run: |
          # 调试：检查 config 目录
          echo "🔍 调试：检查 config 目录"
          ls -R "$GITHUB_WORKSPACE/config" || echo "config 目录不存在"

          # 检查并准备源码
          if [ ! -d "$SOURCE_DIR" ] || [ ! -d "$SOURCE_DIR/.git" ]; then
            echo "📥 克隆源码..."
            rm -rf "$SOURCE_DIR" 2>/dev/null || true
            git clone -b $REPO_BRANCH --single-branch --depth=1 $REPO_URL $SOURCE_DIR
          else
            echo "🔄 检查源码更新..."
            cd $SOURCE_DIR
            CURRENT_BRANCH=$(git branch --show-current)
            if [ "$CURRENT_BRANCH" != "$REPO_BRANCH" ]; then
              echo "分支不同，重新克隆..."
              cd ..
              rm -rf "$SOURCE_DIR"
              git clone -b $REPO_BRANCH --single-branch --depth=1 $REPO_URL $SOURCE_DIR
            else
              git fetch origin $REPO_BRANCH
              LOCAL_COMMIT=$(git rev-parse HEAD)
              REMOTE_COMMIT=$(git rev-parse FETCH_HEAD)
              if [ "$LOCAL_COMMIT" != "$REMOTE_COMMIT" ]; then
                echo "有更新，重置到最新..."
                git reset --hard FETCH_HEAD
              else
                echo "源码已是最新"
              fi
            fi
            cd ..
          fi

          # 设置 feeds
          cd $SOURCE_DIR
          if [ -f "$GITHUB_WORKSPACE/feeds.conf.default" ]; then
            cp "$GITHUB_WORKSPACE/feeds.conf.default" ./feeds.conf.default
          fi

          # 动态管理 feeds.conf.default
          # 使用 jq 解析
          NIKKI_ENABLED=$(jq -r '.enable_nikki // false' "$GITHUB_WORKSPACE/config/plugins.json")
          if [ "$NIKKI_ENABLED" != "True" ] && [ "$NIKKI_ENABLED" != "true" ]; then
            echo "🔧 禁用 nikki feed..."
            sed -i '/src-git nikki/d' feeds.conf.default
            sed -i '/nikki/d' feeds.conf.default
          fi

          # QModem feed
          QN_ENABLED=$(jq -r '.enable_qmodem_next // false' "$GITHUB_WORKSPACE/config/plugins.json")
          Q_ENABLED=$(jq -r '.enable_qmodem // false' "$GITHUB_WORKSPACE/config/plugins.json")
          
          # 归一化判断
          if [[ "$QN_ENABLED" != "True" && "$QN_ENABLED" != "true" ]] && [[ "$Q_ENABLED" != "True" && "$Q_ENABLED" != "true" ]]; then
            echo "🔧 禁用 qmodem feed..."
            sed -i '/qmodem/d' feeds.conf.default
          fi

          # 显示最终的 feeds.conf.default 内容
          echo "📋 当前 feeds.conf.default 内容:"
          cat feeds.conf.default

          # 清理 feeds 缓存
          echo "🧹 清理 feeds 缓存..."
          rm -rf tmp/.config* tmp/.packageinfo tmp/.targetinfo tmp/info tmp/.feeds* 2>/dev/null || true
          
          # 清理禁用的 feeds
          [ "$NIKKI_ENABLED" != "true" ] && rm -rf feeds/nikki* package/feeds/nikki 2>/dev/null || true
          [ "$QN_ENABLED" != "true" ] && [ "$Q_ENABLED" != "true" ] && rm -rf feeds/qmodem* package/feeds/qmodem 2>/dev/null || true

          # feeds update/install
          echo "🔄 更新 feeds..."
          ./scripts/feeds update -a -j $(nproc) || true
          ./scripts/feeds install -a -f -j $(nproc) || true
          
          echo "✅ feeds 处理完成"

      - name: 克隆第三方仓库
        run: |
          cd $SOURCE_DIR
          echo "📥 克隆第三方仓库..."
          
          # NetSpeedTest
          NST_ENABLED=$(jq -r '.enable_netspeedtest // false' "$GITHUB_WORKSPACE/config/plugins.json")
          if [[ "$NST_ENABLED" == "True" || "$NST_ENABLED" == "true" ]] && [ ! -d "package/netspeedtest" ]; then
            echo "📥 克隆 netspeedtest 仓库..."
            git clone https://github.com/sirpdboy/netspeedtest.git -b main package/netspeedtest
            if [ $? -ne 0 ]; then
              echo "❌ netspeedtest 仓库克隆失败"
              exit 1
            fi
          fi
          
          # AT WebServer
          AT_ENABLED=$(jq -r '.enable_at_webserver // false' "$GITHUB_WORKSPACE/config/plugins.json")
          if [[ "$AT_ENABLED" == "True" || "$AT_ENABLED" == "true" ]] && [ ! -d "package/luci-app-at-webserver" ]; then
            echo "📥 克隆 at-webserver 仓库..."
            git clone https://github.com/shi-an/mt5700webui-openwrt-server.git -b main package/luci-app-at-webserver
            if [ $? -ne 0 ]; then
              echo "❌ at-webserver 仓库克隆失败"
              exit 1
            fi
          fi
          
          # Lucky
          LUCKY_ENABLED=$(jq -r '.enable_lucky // false' "$GITHUB_WORKSPACE/config/plugins.json")
          if [[ "$LUCKY_ENABLED" == "True" || "$LUCKY_ENABLED" == "true" ]] && [ ! -d "package/luci-app-lucky" ]; then
            echo "📥 克隆 lucky 仓库..."
            git clone https://github.com/gdy666/luci-app-lucky.git -b main package/luci-app-lucky
            if [ $? -ne 0 ]; then
              echo "❌ lucky 仓库克隆失败"
              exit 1
            fi
          fi
          
          # Daed
          DAED_ENABLED=$(jq -r '.enable_daed // false' "$GITHUB_WORKSPACE/config/plugins.json")
          if [[ "$DAED_ENABLED" == "True" || "$DAED_ENABLED" == "true" ]] && [ ! -d "package/dae" ]; then
            echo "📥 克隆 daed 仓库..."
            git clone https://github.com/QiuSimons/luci-app-daed.git package/dae
            if [ $? -ne 0 ]; then
              echo "❌ daed 仓库克隆失败"
              exit 1
            fi
            # 安装 pnpm (编译前端界面需要)
            sudo npm install -g pnpm
          fi

      - name: 下载 Release 软件包并准备自动安装
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cd $SOURCE_DIR
          # 标准化 files 目录结构
          INSTALL_DIR="files/root/ipks"
          mkdir -p $INSTALL_DIR
          mkdir -p files/etc/uci-defaults
          mkdir -p files/etc/config
          mkdir -p files/etc/init.d
          mkdir -p files/usr/bin
          mkdir -p files/usr/lib
          
          echo "✅ files 目录结构标准化完成"

          # --- 下载 EasyTier ---
          ET_ENABLED=$(jq -r '.enable_easytier // false' "$GITHUB_WORKSPACE/config/plugins.json")
          if [[ "$ET_ENABLED" == "True" || "$ET_ENABLED" == "true" ]]; then
            echo "📥 获取 EasyTier Release URL..."
            ET_URL=$(curl -s -H "Authorization: token $GH_TOKEN" https://api.github.com/repos/EasyTier/luci-app-easytier/releases/latest | jq -r '.assets[] | select(.name | contains("aarch64_cortex-a53") and contains("22.03.7") and (contains("SNAPSHOT") | not)).browser_download_url // empty' | head -1)
            if [ -z "$ET_URL" ] || [ "$ET_URL" = "null" ] || [ "$ET_URL" = "" ]; then
              ET_URL=$(curl -s -H "Authorization: token $GH_TOKEN" https://api.github.com/repos/EasyTier/luci-app-easytier/releases/latest | jq -r '.assets[] | select(.name | contains("aarch64_cortex-a53") and (contains("SNAPSHOT") | not)).browser_download_url // empty' | head -1)
            fi
            if [ -n "$ET_URL" ] && [ "$ET_URL" != "null" ] && [ "$ET_URL" != "" ]; then
              echo "正在下载: $ET_URL"
              wget -qO /tmp/easytier.zip "$ET_URL"
              if [ -f "/tmp/easytier.zip" ] && [ "$(stat -c %s "/tmp/easytier.zip")" -gt 1024 ]; then
                unzip -o /tmp/easytier.zip -d $INSTALL_DIR/
                IPK_FILES=$(ls -la "$INSTALL_DIR"/easytier*.ipk 2>/dev/null | wc -l)
                if [ "$IPK_FILES" -gt 0 ]; then
                  echo "✅ EasyTier 下载并解压成功"
                else
                  echo "❌ EasyTier 解压失败"
                  exit 1
                fi
              else
                echo "❌ EasyTier 下载失败"
                exit 1
              fi
            else
              echo "❌ 无法获取 EasyTier 下载地址"
              exit 1
            fi
          fi

          # --- 下载 Bandix ---
          if [ "$(jq -r '.enable_bandix // false' "$GITHUB_WORKSPACE/config/plugins.json")" = "true" ]; then
            echo "📥 获取 Bandix Release URL..."
            BD_CORE_URL=$(curl -s -H "Authorization: token $GH_TOKEN" https://api.github.com/repos/timsaya/openwrt-bandix/releases/latest | jq -r '.assets[] | select(.name | contains("bandix") and contains("aarch64_cortex-a53") and contains(".ipk")).browser_download_url // empty' | head -1)
            BD_LUCI_URL=$(curl -s -H "Authorization: token $GH_TOKEN" https://api.github.com/repos/timsaya/luci-app-bandix/releases/latest | jq -r '.assets[] | select(.name | contains("luci-app-bandix") and contains(".ipk")).browser_download_url // empty' | head -1)
            BD_I18N_URL=$(curl -s -H "Authorization: token $GH_TOKEN" https://api.github.com/repos/timsaya/luci-app-bandix/releases/latest | jq -r '.assets[] | select(.name | contains("luci-i18n-bandix-zh-cn") and contains(".ipk")).browser_download_url // empty' | head -1)
            BD_LUCI_URLS=""
            [ -n "$BD_LUCI_URL" ] && BD_LUCI_URLS="$BD_LUCI_URLS $BD_LUCI_URL"
            [ -n "$BD_I18N_URL" ] && BD_LUCI_URLS="$BD_LUCI_URLS $BD_I18N_URL"
            
            if [ -n "$BD_CORE_URL" ] && [ "$BD_CORE_URL" != "null" ] && [ "$BD_CORE_URL" != "" ]; then
              echo "正在下载 Bandix 核心: $BD_CORE_URL"
              wget -P $INSTALL_DIR/ "$BD_CORE_URL"
              CORE_FILE=$(basename "$BD_CORE_URL")
              if [ -f "$INSTALL_DIR/$CORE_FILE" ] && [ "$(stat -c %s "$INSTALL_DIR/$CORE_FILE")" -gt 1024 ]; then
                echo "✅ Bandix 核心下载成功"
              else
                echo "❌ Bandix 核心下载失败"
                exit 1
              fi
              
              if [ -n "$BD_LUCI_URLS" ] && [ "$BD_LUCI_URLS" != "" ]; then
                echo "正在下载 Bandix LuCI 包..."
                echo "$BD_LUCI_URLS" | xargs -n 1 wget -P $INSTALL_DIR/
                LUCI_FILES=$(ls -la "$INSTALL_DIR"/luci-app-bandix*.ipk 2>/dev/null | wc -l)
                if [ "$LUCI_FILES" -gt 0 ]; then
                  echo "✅ Bandix LuCI 包下载成功"
                else
                  echo "❌ Bandix LuCI 包下载失败"
                  exit 1
                fi
              else
                echo "⚠️  无法获取 Bandix LuCI 包地址，跳过下载"
              fi
            else
              echo "❌ 无法获取 Bandix 下载地址"
              exit 1
            fi
          fi

          # --- 创建自动安装脚本 ---
          cat > files/etc/uci-defaults/99-install-custom-ipks << 'EOF'
          #!/bin/sh
          if [ -d "/root/ipks" ]; then
            # 优先安装核心依赖，再安装界面
            opkg install /root/ipks/easytier*.ipk
            opkg install /root/ipks/bandix*.ipk
            opkg install /root/ipks/luci-app-*.ipk
            opkg install /root/ipks/luci-i18n-*.ipk
            rm -rf /root/ipks
          fi
          exit 0
          EOF
          chmod +x files/etc/uci-defaults/99-install-custom-ipks

      - name: 插件配置关联
        run: |
          cd $SOURCE_DIR
          touch features.config
          
          if [ "$(jq -r '.enable_bandix // false' "$GITHUB_WORKSPACE/config/plugins.json")" = "true" ]; then
              echo "CONFIG_PACKAGE_zoneinfo-all=y" >> features.config
              echo "CONFIG_PACKAGE_zoneinfo-core=y" >> features.config
              echo "CONFIG_PACKAGE_zoneinfo-europe=y" >> features.config
              echo "CONFIG_PACKAGE_zoneinfo-africa=y" >> features.config
              echo "CONFIG_PACKAGE_zoneinfo-america=y" >> features.config
              echo "CONFIG_PACKAGE_zoneinfo-poles=y" >> features.config
              echo "CONFIG_PACKAGE_zoneinfo-asia=y" >> features.config
              echo "CONFIG_PACKAGE_zoneinfo-atlantic=y" >> features.config
              echo "CONFIG_PACKAGE_zoneinfo-australia-nz=y" >> features.config
              echo "CONFIG_PACKAGE_zoneinfo-pacific=y" >> features.config
              echo "CONFIG_PACKAGE_zoneinfo-indian=y" >> features.config
              echo "CONFIG_PACKAGE_libnetfilter-queue1=y" >> features.config
              echo "CONFIG_PACKAGE_libnetfilter-cttimeout1=y" >> features.config
              echo "CONFIG_PACKAGE_libnetfilter-cthelper0=y" >> features.config
              echo "CONFIG_PACKAGE_conntrack=y" >> features.config
              echo "CONFIG_PACKAGE_kmod-tun=y" >> features.config
              echo "CONFIG_PACKAGE_libstdcpp=y" >> features.config
              echo "CONFIG_PACKAGE_libpthread=y" >> features.config
              echo "CONFIG_PACKAGE_librt=y" >> features.config
          fi
          
          if [ "$(jq -r '.enable_easytier // false' "$GITHUB_WORKSPACE/config/plugins.json")" = "true" ]; then
              echo "CONFIG_PACKAGE_kmod-tun=y" >> features.config
              echo "CONFIG_PACKAGE_libstdcpp=y" >> features.config
              echo "CONFIG_PACKAGE_libpthread=y" >> features.config
              echo "CONFIG_PACKAGE_librt=y" >> features.config
          fi
          
          if [ "$(jq -r '.enable_daed // false' "$GITHUB_WORKSPACE/config/plugins.json")" = "true" ]; then
              echo "CONFIG_PACKAGE_luci-app-daed=y" >> features.config
              echo "CONFIG_PACKAGE_luci-i18n-daed-zh-cn=y" >> features.config
              echo "CONFIG_DEVEL=y" >> features.config
              echo "CONFIG_KERNEL_DEBUG_INFO=y" >> features.config
              echo "CONFIG_KERNEL_DEBUG_INFO_REDUCED=n" >> features.config
              echo "CONFIG_KERNEL_DEBUG_INFO_BTF=y" >> features.config
              echo "CONFIG_KERNEL_CGROUPS=y" >> features.config
              echo "CONFIG_KERNEL_CGROUP_BPF=y" >> features.config
              echo "CONFIG_KERNEL_BPF_EVENTS=y" >> features.config
              echo "CONFIG_BPF_TOOLCHAIN_HOST=y" >> features.config
              echo "CONFIG_KERNEL_XDP_SOCKETS=y" >> features.config
              echo "CONFIG_PACKAGE_kmod-xdp-sockets-diag=y" >> features.config
              echo "CONFIG_PACKAGE_kmod-sched-bpf=y" >> features.config
          fi
          
          if [ "$(jq -r '.enable_nikki // false' "$GITHUB_WORKSPACE/config/plugins.json")" = "true" ]; then
              echo "CONFIG_PACKAGE_nikki=y" >> features.config
              echo "CONFIG_PACKAGE_luci-app-nikki=y" >> features.config
              echo "CONFIG_PACKAGE_luci-i18n-nikki-zh-cn=y" >> features.config
              echo "CONFIG_PACKAGE_ca-bundle=y" >> features.config
              echo "CONFIG_PACKAGE_curl=y" >> features.config
              echo "CONFIG_PACKAGE_yq=y" >> features.config
              echo "CONFIG_PACKAGE_firewall4=y" >> features.config
              echo "CONFIG_PACKAGE_ip-full=y" >> features.config
              echo "CONFIG_PACKAGE_kmod-inet-diag=y" >> features.config
              echo "CONFIG_PACKAGE_kmod-nft-socket=y" >> features.config
              echo "CONFIG_PACKAGE_kmod-nft-tproxy=y" >> features.config
              echo "CONFIG_PACKAGE_kmod-tun=y" >> features.config
          fi
          
          echo "✅ 插件配置关联完成"
          cat features.config

      - name: 修复依赖和补丁
        run: |
          cd $SOURCE_DIR
          echo "🔧 应用补丁和修复依赖..."
          
          # 防止删除 .orig 文件
          sed -i 's/find $targetdir\/ '\''('\'' -name '\''\*.orig'\'' -o -name '\''.\*.orig'\'' '\'')'\'' -exec rm -f {} \\;/echo "Skipping remove .orig files"/' scripts/patch-kernel.sh || true
          
          # 禁用 reject 检查
          sed -i 's/exit 1/echo "Continuing despite rejects"/' scripts/patch-kernel.sh || true
          
          # ⭐ 修复 ebtables 下载问题
          EBTABLES_MAKEFILE="package/network/utils/ebtables/Makefile"
          if [ -f "$EBTABLES_MAKEFILE" ]; then
            sed -i 's|https://git.netfilter.org/ebtables|https://github.com/netfilter/ebtables.git|g' "$EBTABLES_MAKEFILE"
            sed -i 's|git://git.netfilter.org/ebtables|https://github.com/netfilter/ebtables.git|g' "$EBTABLES_MAKEFILE"
            sed -i 's|PKG_MIRROR_HASH:=.*|PKG_MIRROR_HASH:=skip|g' "$EBTABLES_MAKEFILE"
          fi
          
          # ⭐ 修复 QMI WWAN 驱动 Linux 6.6 兼容性
          fix_qmi_driver() {
            local SOURCE_FILE="$1"
            if [ -f "$SOURCE_FILE" ]; then
              echo "🔧 修复驱动: $SOURCE_FILE"
              sed -i 's/u64_stats_fetch_begin_irq/u64_stats_fetch_begin/g' "$SOURCE_FILE"
              sed -i 's/u64_stats_fetch_retry_irq/u64_stats_fetch_retry/g' "$SOURCE_FILE"
              if grep -q 'memcpy.*qmap_net->dev_addr.*real_dev->dev_addr' "$SOURCE_FILE"; then
                sed -i 's/memcpy[[:space:]]*(qmap_net->dev_addr,[[:space:]]*real_dev->dev_addr,[[:space:]]*ETH_ALEN);/eth_hw_addr_set(qmap_net, real_dev->dev_addr);/g' "$SOURCE_FILE"
              fi
              if grep -q 'memcpy.*->dev_addr' "$SOURCE_FILE"; then
                sed -i 's/memcpy[[:space:]]*(\([^,]*\)->dev_addr,[[:space:]]*\([^,]*\),[[:space:]]*ETH_ALEN);/dev_addr_set(\1, \2);/g' "$SOURCE_FILE" 2>/dev/null || true
              fi
            fi
          }
          
          fix_qmi_driver "package/mtk/applications/5g-modem/fibocom_QMI_WWAN/qmi_wwan_f.c"
          fix_qmi_driver "package/mtk/applications/5g-modem/fibocom_QMI_WWAN/src/qmi_wwan_f.c"
          fix_qmi_driver "package/mtk/applications/5g-modem/quectel_QMI_WWAN/qmi_wwan_q.c"
          fix_qmi_driver "package/mtk/applications/5g-modem/quectel_QMI_WWAN/src/qmi_wwan_q.c"
          fix_qmi_driver "package/mtk/applications/5g-modem/simcom_QMI_WWAN/qmi_wwan_s.c"
          fix_qmi_driver "package/mtk/applications/5g-modem/simcom_QMI_WWAN/src/qmi_wwan_s.c"
          
          if [ -d "feeds/qmodem" ]; then
            for driver_file in $(find feeds/qmodem -name "*.c" -type f 2>/dev/null | xargs grep -l "u64_stats_fetch_begin_irq\|memcpy.*dev_addr" 2>/dev/null || true); do
              fix_qmi_driver "$driver_file"
            done
          fi
          
          # 下载额外包 (AdGuardHome, Adbyby Plus, MosDNS)
          AGH_ENABLED=$(jq -r '.enable_adguardhome // false' "$GITHUB_WORKSPACE/config/plugins.json")
          if [[ "$AGH_ENABLED" == "True" || "$AGH_ENABLED" == "true" ]]; then
            [ ! -d "package/luci-app-adguardhome" ] && \
              git clone --depth=1 https://github.com/kongfl888/luci-app-adguardhome.git package/luci-app-adguardhome
            AGH_LUCI_SCRIPT="package/luci-app-adguardhome/root/usr/share/AdGuardHome/links.sh"
            if [ -f "$AGH_LUCI_SCRIPT" ]; then
              sed -i 's|mv /usr/bin/AdGuardHome/AdGuardHome|rm -rf /usr/bin/AdGuardHome 2>/dev/null; mkdir -p /usr/bin/AdGuardHome; mv /tmp/AdGuardHomeupdate/AdGuardHome_linux_*/AdGuardHome|g' "$AGH_LUCI_SCRIPT" || true
            fi
            AGH_LUCI_INIT="package/luci-app-adguardhome/root/etc/init.d/AdGuardHome"
            if [ -f "$AGH_LUCI_INIT" ]; then
              sed -i '/^#!/a\
            [ -f /usr/bin/AdGuardHome ] && rm -f /usr/bin/AdGuardHome && mkdir -p /usr/bin/AdGuardHome' "$AGH_LUCI_INIT" || true
            fi
          fi

          ADBYBY_ENABLED=$(jq -r '.enable_adbyby_plus // false' "$GITHUB_WORKSPACE/config/plugins.json")
          if [[ "$ADBYBY_ENABLED" == "True" || "$ADBYBY_ENABLED" == "true" ]]; then
            echo "🔧 安装 Adbyby Plus Lite..."
            if [ ! -d "package/luci-app-adbyby-plus" ]; then
              git clone --depth=1 --recurse-submodules https://github.com/kongfl888/luci-app-adbyby-plus-lite.git /tmp/adbyby-plus-lite
              cp -r /tmp/adbyby-plus-lite/luci-app-adbyby-plus package/luci-app-adbyby-plus
              rm -rf /tmp/adbyby-plus-lite
            fi
            echo "CONFIG_PACKAGE_ipset=y" >> .config 2>/dev/null || true
          fi

          if [ "$(jq -r '.enable_mosdns // false' "$GITHUB_WORKSPACE/config/plugins.json")" = "true" ]; then
            if [ ! -d "feeds/packages/lang/golang" ] || [ ! -f "feeds/packages/lang/golang/Makefile" ]; then
              rm -rf feeds/packages/lang/golang
              git clone https://github.com/sbwml/packages_lang_golang -b 24.x feeds/packages/lang/golang
            fi
            rm -rf feeds/packages/net/v2ray-geodata
            if [ ! -d "package/mosdns" ]; then
              git clone https://github.com/sbwml/luci-app-mosdns -b v5 package/mosdns
            fi
            if [ ! -d "package/v2ray-geodata" ]; then
              git clone https://github.com/sbwml/v2ray-geodata package/v2ray-geodata
            fi
            echo "CONFIG_PACKAGE_luci-i18n-mosdns-zh-cn=y" >> .config
          fi
          
          rm -rf package/feeds/packages/{exim,onionshare-cli,python-zope-event,python-zope-interface,python-gevent,python-twisted} 2>/dev/null || true

          if [ -d "package/feeds/qmodem" ]; then
            rm -rf package/feeds/qmodem/ndisc6 2>/dev/null || true
            find . -path "*qmodem*Makefile" -exec sed -i 's/+\?kmod-mhi-wwan//g' {} \; 2>/dev/null || true
          fi

          [ -f "package/mtk/drivers/mt_hwifi/Makefile" ] && sed -i 's/+kmod-mt_wifi_osal//g' "package/mtk/drivers/mt_hwifi/Makefile" || true

          # 预装 Clash 内核
          if [ "$(jq -r '.enable_openclash // false' "$GITHUB_WORKSPACE/config/plugins.json")" = "true" ]; then
            echo "📦 安装 OpenClash 依赖..."
            mkdir -p feeds/luci/applications/luci-app-openclash/root/etc/openclash/core
            core_path="feeds/luci/applications/luci-app-openclash/root/etc/openclash/core"
            goe_path="feeds/luci/applications/luci-app-openclash/root/etc/openclash"
            CLASH_META_URL="https://raw.githubusercontent.com/vernesong/OpenClash/core/master/meta/clash-linux-arm64.tar.gz"
            GEOIP_URL="https://github.com/Loyalsoldier/v2ray-rules-dat/releases/latest/download/geoip.dat"
            GEOSITE_URL="https://github.com/Loyalsoldier/v2ray-rules-dat/releases/latest/download/geosite.dat"
            wget -qO- $CLASH_META_URL | tar xOvz > $core_path/clash_meta
            wget -qO- $GEOIP_URL > $goe_path/GeoIP.dat
            wget -qO- $GEOSITE_URL > $goe_path/GeoSite.dat
            chmod +x $core_path/clash_meta
          fi

      - name: 修改默认LAN IP地址
        run: |
          cd $SOURCE_DIR
          echo "🔧 修改默认LAN IP地址为 10.0.0.1..."
          sed -i 's/192.168.6.1/10.0.0.1/g' package/base-files/files/bin/config_generate

      - name: 配置构建
        run: |
          cd $SOURCE_DIR
          echo "⚙️ 配置构建选项..."

          # 从 plugins.json 读取开关并验证互斥
          CFG="$GITHUB_WORKSPACE/config/plugins.json"
          EN_QN=$(jq -r '.enable_qmodem_next // false' "$CFG")
          EN_Q=$(jq -r '.enable_qmodem // false' "$CFG")
          EN_OM=$(jq -r '.enable_original_modem // false' "$CFG")

          if [ "$EN_QN" = "true" ] && [ "$EN_Q" = "true" ]; then
            echo "❌ 错误: 不能同时启用 QModem Next 和 QModem！"
            exit 1
          fi
          if [ "$EN_OM" = "true" ] && [ "$EN_QN" = "true" ]; then
            echo "❌ 错误: 不能同时启用原版 Modem 和 QModem Next！"
            exit 1
          fi
          if [ "$EN_OM" = "true" ] && [ "$EN_Q" = "true" ]; then
            echo "❌ 错误: 不能同时启用原版 Modem 和 QModem！"
            exit 1
          fi

          curl -fsSL "$CONFIG_URL" -o base.config || {
            [ -f "defconfig/mt7987_mt7992.config" ] && cp defconfig/mt7987_mt7992.config base.config
          }

          cat base.config > .config

          is_enabled() {
            [ "$(jq -r --arg key "$1" '.[$key] // false' "$CFG")" = "true" ]
          }

          disable_package() {
            local pkg="$1"
            sed -i "/^CONFIG_PACKAGE_${pkg}=/d; /^# CONFIG_PACKAGE_${pkg} is not set/d" .config
            echo "# CONFIG_PACKAGE_${pkg} is not set" >> .config
          }

          verify_enabled_pkg() {
            local feature_name="$1"
            local config_name="$2"
            local enabled_key="$3"
            if is_enabled "$enabled_key" && ! grep -q "^CONFIG_PACKAGE_${config_name}=y$" .config; then
              echo "❌ ${feature_name} 已启用，但 make defconfig 后 CONFIG_PACKAGE_${config_name}=y 未生效"
              grep -n "PACKAGE_${config_name}" .config || true
              exit 1
            fi
          }

          # [修复] 如果未启用任何 Modem 插件，显式禁用默认的 luci-app-modem
          # 因为 base.config 默认开启了它，若不处理会导致"自动启用"
          if [ "$EN_QN" != "true" ] && [ "$EN_Q" != "true" ] && [ "$EN_OM" != "true" ]; then
            echo "ℹ️ 未启用任何 Modem，显式禁用原版 Modem"
            disable_package "luci-app-modem"
            disable_package "modem"
            disable_package "luci-i18n-modem-zh-cn"
            disable_package "luci-i18n-modem-en"
          fi

          echo "🔧 基于 plugins.json 循环生成 .config..."
          TRUE_KEYS=$(jq -r 'to_entries | map(select(.key | startswith("enable_")) | select(.value == true or .value == "true") | .key) | join(" ")' "$CFG")
          
          for key in $TRUE_KEYS; do
            name="${key#enable_}"
            case "$name" in
              easytier) echo "ℹ️ $name 通过离线包安装" ;;
              bandix)
                echo "CONFIG_PACKAGE_zoneinfo-all=y" >> .config
                echo "CONFIG_PACKAGE_zoneinfo-core=y" >> .config
                echo "CONFIG_PACKAGE_zoneinfo-europe=y" >> .config
                echo "CONFIG_PACKAGE_zoneinfo-africa=y" >> .config
                echo "CONFIG_PACKAGE_zoneinfo-america=y" >> .config
                echo "CONFIG_PACKAGE_zoneinfo-poles=y" >> .config
                echo "CONFIG_PACKAGE_zoneinfo-asia=y" >> .config
                echo "CONFIG_PACKAGE_zoneinfo-atlantic=y" >> .config
                echo "CONFIG_PACKAGE_zoneinfo-australia-nz=y" >> .config
                echo "CONFIG_PACKAGE_zoneinfo-pacific=y" >> .config
                echo "CONFIG_PACKAGE_zoneinfo-indian=y" >> .config
                echo "CONFIG_PACKAGE_libnetfilter-queue1=y" >> .config
                echo "CONFIG_PACKAGE_libnetfilter-cttimeout1=y" >> .config
                echo "CONFIG_PACKAGE_libnetfilter-cthelper0=y" >> .config
                echo "CONFIG_PACKAGE_conntrack=y" >> .config
                echo "CONFIG_PACKAGE_kmod-tun=y" >> .config
                echo "CONFIG_PACKAGE_libstdcpp=y" >> .config
                echo "CONFIG_PACKAGE_libpthread=y" >> .config
                echo "CONFIG_PACKAGE_librt=y" >> .config
                ;;
              daed)
                echo "CONFIG_PACKAGE_luci-app-daed=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-daed-zh-cn=y" >> .config
                echo "CONFIG_DEVEL=y" >> .config
                echo "CONFIG_KERNEL_DEBUG_INFO=y" >> .config
                echo "CONFIG_KERNEL_DEBUG_INFO_REDUCED=n" >> .config
                echo "CONFIG_KERNEL_DEBUG_INFO_BTF=y" >> .config
                echo "CONFIG_KERNEL_CGROUPS=y" >> .config
                echo "CONFIG_KERNEL_CGROUP_BPF=y" >> .config
                echo "CONFIG_KERNEL_BPF_EVENTS=y" >> .config
                echo "CONFIG_BPF_TOOLCHAIN_HOST=y" >> .config
                echo "CONFIG_KERNEL_XDP_SOCKETS=y" >> .config
                echo "CONFIG_PACKAGE_kmod-xdp-sockets-diag=y" >> .config
                echo "CONFIG_PACKAGE_kmod-sched-bpf=y" >> .config
                echo "CONFIG_KERNEL_BPF=y" >> .config
                echo "CONFIG_KERNEL_BPF_SYSCALL=y" >> .config
                echo "CONFIG_KERNEL_BPF_JIT=y" >> .config
                echo "CONFIG_KERNEL_KPROBES=y" >> .config
                echo "CONFIG_KERNEL_NET_INGRESS=y" >> .config
                echo "CONFIG_KERNEL_NET_EGRESS=y" >> .config
                echo "CONFIG_KERNEL_NET_SCH_INGRESS=m" >> .config
                echo "CONFIG_KERNEL_NET_CLS_BPF=m" >> .config
                echo "CONFIG_KERNEL_NET_CLS_ACT=y" >> .config
                echo "CONFIG_KERNEL_BPF_STREAM_PARSER=y" >> .config
                echo "CONFIG_KERNEL_KPROBE_EVENTS=y" >> .config
                ;;
              etherwake)
                echo "CONFIG_PACKAGE_luci-app-wol=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-wol-zh-cn=y" >> .config
                echo "CONFIG_PACKAGE_etherwake=y" >> .config
                ;;
              wireguard)
                echo "CONFIG_PACKAGE_luci-proto-wireguard=y" >> .config
                echo "CONFIG_PACKAGE_luci-app-wireguard=y" >> .config
                echo "CONFIG_PACKAGE_wireguard-tools=y" >> .config
                echo "CONFIG_PACKAGE_kmod-wireguard=y" >> .config
                ;;
              nikki)
                disable_package "mihomo-alpha"
                echo "CONFIG_PACKAGE_mihomo-meta=y" >> .config
                echo "CONFIG_PACKAGE_nikki=y" >> .config
                echo "CONFIG_PACKAGE_luci-app-nikki=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-nikki-zh-cn=y" >> .config
                echo "CONFIG_PACKAGE_ca-bundle=y" >> .config
                echo "CONFIG_PACKAGE_curl=y" >> .config
                echo "CONFIG_PACKAGE_yq=y" >> .config
                echo "CONFIG_PACKAGE_firewall4=y" >> .config
                echo "CONFIG_PACKAGE_ip-full=y" >> .config
                echo "CONFIG_PACKAGE_kmod-inet-diag=y" >> .config
                echo "CONFIG_PACKAGE_kmod-nft-socket=y" >> .config
                echo "CONFIG_PACKAGE_kmod-nft-tproxy=y" >> .config
                echo "CONFIG_PACKAGE_kmod-tun=y" >> .config
                ;;
              openclash)
                echo "CONFIG_PACKAGE_luci-app-openclash=y" >> .config
                ;;
              adbyby_plus)
                echo "CONFIG_PACKAGE_luci-app-adbyby-plus=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-adbyby-plus-zh-cn=y" >> .config
                echo "CONFIG_PACKAGE_ipset=y" >> .config
                ;;
              original_modem)
                echo "CONFIG_PACKAGE_luci-app-modem=y" >> .config
                echo "CONFIG_PACKAGE_modem=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-modem-zh-cn=y" >> .config
                echo "# CONFIG_PACKAGE_luci-app-qmodem-next is not set" >> .config
                echo "# CONFIG_PACKAGE_luci-app-qmodem is not set" >> .config
                echo "# CONFIG_PACKAGE_qmodem is not set" >> .config
                ;;
              qmodem_next)
                echo "CONFIG_PACKAGE_luci-app-qmodem-next=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-qmodem-next-zh-cn=y" >> .config
                echo "# CONFIG_PACKAGE_luci-app-qmodem is not set" >> .config
                echo "# CONFIG_PACKAGE_qmodem is not set" >> .config
                echo "# CONFIG_PACKAGE_luci-app-modem is not set" >> .config
                ;;
              qmodem)
                echo "CONFIG_PACKAGE_luci-app-qmodem=y" >> .config
                echo "CONFIG_PACKAGE_luci-compat=y" >> .config
                echo "CONFIG_PACKAGE_qmodem=y" >> .config
                echo "# CONFIG_PACKAGE_luci-app-qmodem-next is not set" >> .config
                echo "# CONFIG_PACKAGE_luci-app-modem is not set" >> .config
                echo "CONFIG_PACKAGE_luci-app-qmodem_INCLUDE_vendor-qmi-wwan=y" >> .config
                echo "# CONFIG_PACKAGE_luci-app-qmodem_INCLUDE_generic-qmi-wwan is not set" >> .config
                echo "CONFIG_PACKAGE_luci-app-qmodem_INCLUDE_ndisc6=y" >> .config
                echo "# CONFIG_PACKAGE_luci-app-qmodem_INCLUDE_rdisc6 is not set" >> .config
                echo "# CONFIG_PACKAGE_luci-app-qmodem_INCLUDE_no_ndisc_rdisc6 is not set" >> .config
                echo "CONFIG_PACKAGE_ndisc6=y" >> .config
                echo "CONFIG_PACKAGE_luci-app-qmodem_USE_TOM_CUSTOMIZED_QUECTEL_CM=y" >> .config
                echo "# CONFIG_PACKAGE_luci-app-qmodem_USING_QWRT_QUECTEL_CM_5G is not set" >> .config
                echo "# CONFIG_PACKAGE_luci-app-qmodem_USING_NORMAL_QUECTEL_CM is not set" >> .config
                echo "CONFIG_PACKAGE_quectel-CM-5G-M=y" >> .config
                echo "CONFIG_PACKAGE_sms-tool_q=y" >> .config
                echo "CONFIG_PACKAGE_ubus-at-daemon=y" >> .config
                echo "CONFIG_PACKAGE_tom_modem=y" >> .config
                echo "CONFIG_PACKAGE_luci-app-qmodem-sms=y" >> .config
                echo "CONFIG_PACKAGE_kmod-qmi_wwan_q=y" >> .config
                echo "CONFIG_PACKAGE_kmod-qmi_wwan_f=y" >> .config
                echo "CONFIG_PACKAGE_kmod-qmi_wwan_s=y" >> .config
                echo "CONFIG_PACKAGE_mtkhqos_util=y" >> .config
                ;;
              mwan)
                echo "CONFIG_PACKAGE_mwan3=y" >> .config
                echo "CONFIG_PACKAGE_luci-app-mwan3=y" >> .config
                ;;
              zerotier)
                echo "CONFIG_PACKAGE_zerotier=y" >> .config
                echo "CONFIG_PACKAGE_luci-app-zerotier=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-zerotier-zh-cn=y" >> .config
                ;;
              wrtbwmon)
                echo "CONFIG_PACKAGE_wrtbwmon=y" >> .config
                echo "CONFIG_PACKAGE_luci-app-wrtbwmon=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-wrtbwmon-zh-cn=y" >> .config
                ;;
              watchcat)
                echo "CONFIG_PACKAGE_watchcat=y" >> .config
                echo "CONFIG_PACKAGE_luci-app-watchcat=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-watchcat-zh-cn=y" >> .config
                ;;
              netspeedtest)
                echo "CONFIG_PACKAGE_luci-app-netspeedtest=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-netspeedtest-zh-cn=y" >> .config
                ;;
              at_webserver)
                # 👇 显式选中我们重构的 Rust 后端核心包
                echo "CONFIG_PACKAGE_at-webserver=y" >> .config
                # 选中前端界面
                echo "CONFIG_PACKAGE_luci-app-at-webserver=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-at-webserver-zh-cn=y" >> .config
                ;;
              lucky)
                echo "CONFIG_PACKAGE_luci-app-lucky=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-lucky-zh-cn=y" >> .config
                ;;
              dockerman)
                echo "CONFIG_PACKAGE_luci-app-dockerman=y" >> .config
                echo "CONFIG_PACKAGE_luci-lib-docker=y" >> .config
                echo "CONFIG_PACKAGE_docker=y" >> .config
                echo "CONFIG_PACKAGE_dockerd=y" >> .config
                echo "CONFIG_PACKAGE_containerd=y" >> .config
                echo "CONFIG_PACKAGE_runc=y" >> .config
                ;;
              vlmcsd)
                echo "CONFIG_PACKAGE_luci-app-vlmcsd=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-vlmcsd-zh-cn=y" >> .config
                echo "CONFIG_PACKAGE_vlmcsd=y" >> .config
                ;;
              adguardhome|mosdns|homeproxy|upnp)
                echo "CONFIG_PACKAGE_luci-app-${name}=y" >> .config
                echo "CONFIG_PACKAGE_luci-i18n-${name}-zh-cn=y" >> .config || true
                ;;
              *)
                echo "CONFIG_PACKAGE_luci-app-${name}=y" >> .config
                ;;
            esac
          done

          if [ -f "$GITHUB_WORKSPACE/h5000m.extra.config" ]; then
            echo "📄 应用额外配置..."
            cat "$GITHUB_WORKSPACE/h5000m.extra.config" >> .config
          fi

          all_disabled=(
            "luci-app-sms-tool-lite"
            "luci-app-3ginfo-lite"
            "luci-app-wrtbwmon"
            "wrtbwmon"
            "luci-app-rclone"
            "rclone"
            "rclone-ng"
            "rclone-webui-react"
          )

          is_enabled "enable_adguardhome" || all_disabled+=("luci-app-adguardhome" "luci-i18n-adguardhome-zh-cn")
          is_enabled "enable_openclash" || all_disabled+=("luci-app-openclash")
          is_enabled "enable_nikki" || all_disabled+=("nikki" "luci-app-nikki" "luci-i18n-nikki-zh-cn" "luci-i18n-nikki-en" "mihomo-alpha" "mihomo-meta")
          is_enabled "enable_upnp" || all_disabled+=("luci-app-upnp" "luci-i18n-upnp-zh-cn")
          is_enabled "enable_vlmcsd" || all_disabled+=("luci-app-vlmcsd" "luci-i18n-vlmcsd-zh-cn" "vlmcsd")
          is_enabled "enable_mosdns" || all_disabled+=("luci-app-mosdns" "luci-i18n-mosdns-zh-cn")
          is_enabled "enable_dockerman" || all_disabled+=("luci-app-dockerman" "luci-lib-docker" "docker" "dockerd" "containerd" "runc")
          is_enabled "enable_qmodem_next" || all_disabled+=("luci-app-qmodem-next" "luci-i18n-qmodem-next-zh-cn")
          is_enabled "enable_qmodem" || all_disabled+=("luci-app-qmodem" "qmodem" "luci-app-qmodem-sms")
          is_enabled "enable_mwan" || all_disabled+=("luci-app-mwan3")
          is_enabled "enable_homeproxy" || all_disabled+=("luci-app-homeproxy" "luci-i18n-homeproxy-zh-cn")
          is_enabled "enable_adbyby_plus" || all_disabled+=("luci-app-adbyby-plus" "luci-i18n-adbyby-plus-zh-cn")
          is_enabled "enable_original_modem" || all_disabled+=("luci-app-modem" "modem" "luci-i18n-modem-zh-cn" "luci-i18n-modem-en")
          is_enabled "enable_netspeedtest" || all_disabled+=("luci-app-netspeedtest" "luci-i18n-netspeedtest-zh-cn")
          is_enabled "enable_at_webserver" || all_disabled+=("at-webserver" "luci-app-at-webserver" "luci-i18n-at-webserver-zh-cn")
          is_enabled "enable_zerotier" || all_disabled+=("zerotier" "luci-app-zerotier" "luci-i18n-zerotier-zh-cn")
          is_enabled "enable_watchcat" || all_disabled+=("watchcat" "luci-app-watchcat" "luci-i18n-watchcat-zh-cn")
          is_enabled "enable_lucky" || all_disabled+=("luci-app-lucky" "luci-i18n-lucky-zh-cn")
          is_enabled "enable_daed" || all_disabled+=("luci-app-daed" "luci-i18n-daed-zh-cn")
          is_enabled "enable_etherwake" || all_disabled+=("luci-app-wol" "luci-i18n-wol-zh-cn" "etherwake")
          is_enabled "enable_wireguard" || all_disabled+=("luci-proto-wireguard" "luci-app-wireguard" "wireguard-tools" "kmod-wireguard")

          for pkg in "${all_disabled[@]}"; do
             disable_package "$pkg"
          done

          make defconfig

          verify_enabled_pkg "Nikki" "luci-app-nikki" "enable_nikki"
          verify_enabled_pkg "AdGuardHome" "luci-app-adguardhome" "enable_adguardhome"
          verify_enabled_pkg "OpenClash" "luci-app-openclash" "enable_openclash"
          verify_enabled_pkg "UPnP" "luci-app-upnp" "enable_upnp"
          verify_enabled_pkg "VLMCSd" "luci-app-vlmcsd" "enable_vlmcsd"
          verify_enabled_pkg "MosDNS" "luci-app-mosdns" "enable_mosdns"
          verify_enabled_pkg "DockerMan" "luci-app-dockerman" "enable_dockerman"
          verify_enabled_pkg "QModem Next" "luci-app-qmodem-next" "enable_qmodem_next"
          verify_enabled_pkg "QModem" "luci-app-qmodem" "enable_qmodem"
          verify_enabled_pkg "Original Modem" "luci-app-modem" "enable_original_modem"
          verify_enabled_pkg "MWAN" "luci-app-mwan3" "enable_mwan"
          verify_enabled_pkg "HomeProxy" "luci-app-homeproxy" "enable_homeproxy"
          verify_enabled_pkg "Adbyby Plus" "luci-app-adbyby-plus" "enable_adbyby_plus"
          verify_enabled_pkg "NetSpeedTest" "luci-app-netspeedtest" "enable_netspeedtest"
          verify_enabled_pkg "AT WebServer" "luci-app-at-webserver" "enable_at_webserver"
          verify_enabled_pkg "ZeroTier" "luci-app-zerotier" "enable_zerotier"
          verify_enabled_pkg "WatchCat" "luci-app-watchcat" "enable_watchcat"
          verify_enabled_pkg "Lucky" "luci-app-lucky" "enable_lucky"
          verify_enabled_pkg "Daed" "luci-app-daed" "enable_daed"
          verify_enabled_pkg "EtherWake" "luci-app-wol" "enable_etherwake"
          verify_enabled_pkg "WireGuard" "luci-app-wireguard" "enable_wireguard"

          echo "✅ 配置完成"

      - name: 上传构建配置 Artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-configs
          path: config/
          retention-days: 7

      - name: 编译固件
        run: |
          cd $SOURCE_DIR
          THREADS=$(nproc)
          export PATH="/usr/lib/ccache:$PATH"
          
          echo "🔨 开始编译 (${THREADS} 线程)..."
          echo "📊 CCache 统计 (开始):"
          ccache -s || true
          
          START_TIME=$(date +%s)
          
          echo "📥 下载构建依赖..."
          find dl -size -1024c -delete 2>/dev/null || true
          make download -j$THREADS V=s || { find dl -size -1024c -delete 2>/dev/null || true; make download -j1 V=s; }
          
          echo "🔧 预构建工具链..."
          [ -d "staging_dir" ] && [ -n "$(ls -A staging_dir 2>/dev/null)" ] && echo "✅ 工具链已缓存" || make toolchain/install -j$THREADS || true
          
          if make -j$THREADS IGNORE_ERRORS=n; then
            END_TIME=$(date +%s)
            DURATION=$((END_TIME - START_TIME))
            echo "✅ 编译成功 (耗时: ${DURATION}s)"
            echo "📊 CCache 统计 (结束):"
            ccache -s || true
          else
            echo "⚠️ 并行编译失败，尝试单线程调试..."
            make -j1 V=s || {
              echo "❌ 编译失败"
              exit 1
            }
          fi

      - name: 打包产物
        run: |
          cd $SOURCE_DIR
          echo "📦 打包产物..."
          mkdir -p ../artifacts
          find bin/targets -type f \( -name "*.bin" -o -name "*.img.gz" \) -exec cp {} ../artifacts/ \;
          cd ..
          tar -czf artifacts.tar.gz artifacts/

      - name: 动态生成Release信息
        run: |
          cd $SOURCE_DIR
          echo "🎉 **ImmortalWrt H5000M 固件已构建完成**" > release_body.md
          echo "" >> release_body.md
          echo "📅 时间: ${{ github.event.repository.updated_at }}" >> release_body.md
          echo "🔗 运行 ID: ${{ github.run_id }}" >> release_body.md
          echo "" >> release_body.md
          echo "**⚙️ 启用的功能:**" >> release_body.md
          
          jq -r 'to_entries | map(select(.key | startswith("enable_")) | select(.value == true or .value == "true") | .key | sub("^enable_"; "")) | .[]' "$GITHUB_WORKSPACE/config/plugins.json" | awk '
            {
              key=$0;
              if (key=="adguardhome") name="AdGuardHome";
              else if (key=="openclash") name="OpenClash";
              else if (key=="nikki") name="Nikki";
              else if (key=="upnp") name="UPnP";
              else if (key=="vlmcsd") name="VLMCSd";
              else if (key=="mosdns") name="MosDNS";
              else if (key=="dockerman") name="DockerMan";
              else if (key=="qmodem_next") name="QModem Next";
              else if (key=="qmodem") name="QModem";
              else if (key=="mwan") name="MWAN";
              else if (key=="homeproxy") name="HomeProxy";
              else if (key=="adbyby_plus") name="Adbyby Plus";
              else if (key=="original_modem") name="Original Modem";
              else if (key=="netspeedtest") name="NetSpeedTest";
              else if (key=="at_webserver") name="AT WebServer";
              else if (key=="zerotier") name="ZeroTier";
              else if (key=="watchcat") name="WatchCat";
              else if (key=="easytier") name="EasyTier";
              else if (key=="lucky") name="Lucky";
              else if (key=="bandix") name="Bandix";
              else if (key=="daed") name="Daed";
              else if (key=="turboacc") name="TurboACC";
              else if (key=="eqos_mtk") name="EQOS-MTK";
              else if (key=="etherwake") name="EtherWake";
              else if (key=="wireguard") name="WireGuard";
              else { name=key; gsub(/_/, " ", name); }
              print "- ✅ " name;
            }
          ' >> release_body.md
          
          echo "" >> release_body.md
          echo "💾 下载产物: 见下方 Artifacts" >> release_body.md
          echo "📋 详细信息: 见 MANIFEST.txt" >> release_body.md
          
          cp release_body.md ../artifacts/
          echo "✅ Release信息生成完成"

      - name: 验证产物
        run: |
          echo "🔍 验证产物..."
          if [ -z "$(ls -A artifacts/ 2>/dev/null)" ]; then
            echo "❌ 没有找到产物文件"
            exit 1
          else
            echo "✅ 产物验证通过"
            ls -lh artifacts/
          fi

      - name: 上传产物
        uses: actions/upload-artifact@v4
        with:
          name: artifacts-${{ github.run_number }}
          path: artifacts.tar.gz
          retention-days: 30

      - name: 缓存插件配置保存
        uses: actions/cache/save@v4
        if: always()
        with:
          path: config/plugins.json
          key: plugins-config-${{ github.run_id }}

  release:
    needs: build_firmware
    runs-on: ubuntu-22.04
    if: success()
    steps:
      - name: 下载构建配置
        uses: actions/download-artifact@v4
        with:
          name: build-configs
          path: config

      - name: 下载产物
        uses: actions/download-artifact@v4
        with:
          name: artifacts-${{ github.run_number }}

      - name: 解压产物
        run: |
          echo "📦 解压产物..."
          tar -xzf artifacts.tar.gz
          echo "✅ 产物解压完成"

      - name: 收集和上传产物
        run: |
          if [ -z "$(ls -A artifacts/ 2>/dev/null)" ]; then
            echo "❌ 没有找到产物文件"
            exit 1
          fi

          echo "🎉 **ImmortalWrt H5000M 固件已构建完成**" > artifacts/release_body.md
          echo "" >> artifacts/release_body.md
          echo "📅 时间: $(date '+%Y-%m-%d %H:%M:%S')" >> artifacts/release_body.md
          echo "🔗 运行 ID: ${{ github.run_id }}" >> artifacts/release_body.md
          echo "" >> artifacts/release_body.md
          echo "**⚙️ 启用的功能:**" >> artifacts/release_body.md
          
          jq -r 'to_entries | map(select(.key | startswith("enable_")) | select(.value == true or .value == "true") | .key | sub("^enable_"; "")) | .[]' "$GITHUB_WORKSPACE/config/plugins.json" | awk '
            {
              key=$0;
              if (key=="adguardhome") name="AdGuardHome";
              else if (key=="openclash") name="OpenClash";
              else if (key=="nikki") name="Nikki";
              else if (key=="upnp") name="UPnP";
              else if (key=="vlmcsd") name="VLMCSd";
              else if (key=="mosdns") name="MosDNS";
              else if (key=="dockerman") name="DockerMan";
              else if (key=="qmodem_next") name="QModem Next";
              else if (key=="qmodem") name="QModem";
              else if (key=="mwan") name="MWAN";
              else if (key=="homeproxy") name="HomeProxy";
              else if (key=="adbyby_plus") name="Adbyby Plus";
              else if (key=="original_modem") name="Original Modem";
              else if (key=="netspeedtest") name="NetSpeedTest";
              else if (key=="at_webserver") name="AT WebServer";
              else if (key=="zerotier") name="ZeroTier";
              else if (key=="watchcat") name="WatchCat";
              else if (key=="easytier") name="EasyTier";
              else if (key=="lucky") name="Lucky";
              else if (key=="bandix") name="Bandix";
              else if (key=="daed") name="Daed";
              else if (key=="turboacc") name="TurboACC";
              else if (key=="eqos_mtk") name="EQOS-MTK";
              else if (key=="etherwake") name="EtherWake";
              else if (key=="wireguard") name="WireGuard";
              else { name=key; gsub(/_/, " ", name); }
              print "- ✅ " name;
            }
          ' >> artifacts/release_body.md
          
          echo "" >> artifacts/release_body.md
          echo "💾 下载产物: 见下方 Artifacts" >> artifacts/release_body.md
          echo "📋 详细信息: 见 MANIFEST.txt" >> artifacts/release_body.md

          cat > artifacts/MANIFEST.txt << EOF
          ╔════════════════════════════════════════════════╗
          ║    ImmortalWrt H5000M 固件构建信息             ║
          ╚════════════════════════════════════════════════╝

          📅 构建时间: $(date '+%Y-%m-%d %H:%M:%S')
          🔗 运行 ID: ${{ github.run_id }}
          🏷️  Build #: ${{ github.run_number }}

          EOF
          
          echo "" >> artifacts/MANIFEST.txt
          echo "📦 产物文件:" >> artifacts/MANIFEST.txt

          cd artifacts
          ls -lh | grep -v "^total" | awk '{print "  • " $9 " (" $5 ")"}' >> MANIFEST.txt

          echo "✅ 产物收集完成"

      - name: 上传构建结果
        uses: actions/upload-artifact@v4
        with:
          name: ImmortalWrt-H5000M-${{ github.run_number }}
          path: artifacts/
          retention-days: 30

      - name: 创建 GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: h5000m-${{ github.run_number }}
          name: ImmortalWrt H5000M
          body_path: artifacts/release_body.md
          files: |
            artifacts/*.bin
            artifacts/*.img.gz
            artifacts/MANIFEST.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
